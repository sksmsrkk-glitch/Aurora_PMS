import PmsShell from "../_components/pms-shell";

/** Bookmarkable accounting workspace entry point. */
export default function AccountingPage() {
  return <PmsShell initialSection="accounting" />;
}
